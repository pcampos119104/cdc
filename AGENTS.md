# Build/Lint/Test Commands

## Full Test Suite
just test

## Single Test File
pytest cdc/base/tests/test_view.py

## Single Test Method
pytest cdc/base/tests/test_view.py::TestBaseViews::test_home

## Linting & Formatting
just format

## Django Management Commands
just mng makemigrations
just mng migrate
just mng collectstatic

# Code Style Guidelines

## Python
- Line length: 120 characters
- Quote style: single quotes
- Imports: isort (I) rules via ruff
- Code style: pycodestyle (E) rules via ruff
- Never edit migration files in migrations/ folders; they are auto-generated by Django. Only modify if explicitly pointed to do so.
- Exclude: tests/ and **/migrations/** from linting

## Django/Wagtail
- Use snake_case for variables and functions
- Use PascalCase for classes and models
- Follow Django model conventions with verbose_name in Portuguese
- Use RichTextField for HTML content
- Use ClusterTaggableManager for tags
- Follow Wagtail Page patterns for CMS pages
- RecipeTagIndexPage template: 'recipes/recipe_tag_index_page.html'
- Page hierarchy: RecipeTagIndexPage should be child of Home page (not RecipeIndexPage)

## Testing
- Use pytest with django_db fixture for database access
- Wagtail pages are tested via URL paths, not named URLs
- Comprehensive test coverage for all models and views
- Tests organized by functionality: test_models.py, test_views.py, test_snippets.py

# Architecture Overview

This is a Django-based web application using Wagtail as a Content Management System (CMS) for managing a recipe website named "Cozinha de Campos." The project follows standard Django/Wagtail patterns for structure, with a focus on recipe content creation, tagging, and AI-assisted processing. Key architectural elements include core framework components, app structure, page hierarchy, backend processing, authentication, database, file storage, frontend templates, deployment, and development tools.

The app structure includes cdc.base for basic CMS pages like HomePage, and cdc.recipes for recipe management with models like RecipeIndexPage, RecipeTagIndexPage, RecipePage, and AIProcessingTask. Recipes are processed asynchronously using background jobs, supporting tagging, search, and workflows. The page hierarchy follows Wagtail conventions with HomePage as root, RecipeIndexPage as child, and RecipePage instances as grandchildren.

Backend processing uses steady-queue for tasks like AI processing of recipes. Authentication leverages django-allauth for email/username-based login. Database is PostgreSQL, file storage uses local filesystem in debug mode and AWS S3 in production. Frontend uses Django templates with Wagtail's rich text fields. Deployment includes Gunicorn and Whitenoise, with environment variables for configuration.

# Full Tech Stack

- Python
- Django
- Wagtail
- PostgreSQL
- django-allauth
- django-htmx
- whitenoise
- django-storages[s3]
- steady-queue
- gunicorn
- django-environ
- django-extensions
- pytest
- pytest-django
- ruff
- django-browser-reload
- marimo

# Environment Variables

- DEBUG: Enable/disable debug mode
- SECRET_KEY: Django secret key for security
- ALLOWED_HOSTS: Comma-separated list of allowed hosts
- DB_NAME: PostgreSQL database name
- DB_USER: PostgreSQL database user
- DB_PASSWORD: PostgreSQL database password
- DB_HOST: PostgreSQL database host
- DB_PORT: PostgreSQL database port
- CSRF_TRUSTED_ORIGINS: Trusted origins for CSRF protection
- SENTRY_DSN: Sentry error tracking DSN
- PORT: Application port
- AWS_STORAGE_BUCKET_NAME: AWS S3 bucket name
- AWS_S3_ENDPOINT_URL: AWS S3 endpoint URL
- AWS_ACCESS_KEY_ID: AWS access key ID
- AWS_SECRET_ACCESS_KEY: AWS secret access key

# Content Directory Structure

cdc/                    # Main Django project directory
  .env                  # Environment variables file for local configuration
  .env.template         # Template for environment variables
  .git/                 # Git repository metadata
  .github/
    workflows/          # GitHub Actions CI/CD pipelines
  .gitignore            # Files and directories ignored by Git
  .idea/                # PyCharm IDE configuration
  .pytest_cache/        # Pytest cache files
  .ruff_cache/          # Ruff linter cache
  .venv/                # Python virtual environment
  AGENTS.md             # Instructions for AI agents or collaborators
  cdc/
    __init__.py         # Marks directory as Python package
    asgi.py             # ASGI configuration for Django
    base/
      __init__.py       # Marks base app as Python package
      admin.py          # Django admin configuration for base app
      apps.py           # App configuration for base
      migrations/       # Database migration files for base app
      models.py         # Django models for base app
      templatetags/     # Custom Django template tags
      tests/
        __init__.py     # Marks tests as Python package
        test_models.py  # Tests for base models
        test_view.py    # Tests for base views
    recipes/
      __init__.py       # Marks recipes app as Python package
      admin.py          # Django admin for recipes
      apps.py           # App configuration for recipes
      migrations/       # Database migrations for recipes
      models.py         # Django models for recipes
      tasks.py          # Celery tasks for recipes
      tests.py          # Additional tests for recipes
      tests/
        test_models.py  # Tests for recipes models
        test_snippets.py# Tests for recipes snippets
        test_views.py   # Tests for recipes views
      urls.py           # URL patterns for recipes app
      views.py          # Django views for recipes
    settings.py         # Django project settings
    static/
      css/              # Static CSS files
      js/               # Static JavaScript files
    templates/
      base/             # Base HTML templates
      recipes/          # Recipes-specific templates
    urls.py             # Main URL configuration
    wsgi.py            # WSGI configuration for Django
  compose.yaml          # Docker Compose configuration
  docker/
    dev/                # Development Docker setup
    prod/               # Production Docker setup
  extras/
    __marimo__/         # Marimo notebook data
    local/              # Local development extras
    template.py         # Template script
  justfile              # Just command runner recipes
  manage.py             # Django management script
  media/                # User-uploaded media files
  node_modules/         # Node.js dependencies
  notes.md              # Project notes
  package-lock.json     # NPM dependency lock file
  package.json          # NPM package configuration
  prod.Dockerfile.dockerignore  # Docker ignore for production build
  pyproject.toml        # Python project configuration (poetry/uv)
  README.md             # Project documentation
  staticfiles/          # Collected static files for Django
  tmuxp.yaml            # Tmux session configuration
  uv.lock               # UV dependency lock file

# Django Apps: Services, Jobs, and Models

## cdc.base
- **Models**: HomePage (Wagtail Page with RichTextField body)
- **Services/Jobs**: None identified

## cdc.recipes
- **Models**: RecipeIndexPage, RecipeTagIndexPage, RecipePageTag, RecipePage, AIProcessingTask
- **Services/Jobs**: process_recipe_with_ai (asynchronous task for AI processing via steady-queue)